

- 기존에 웹은 어떻게 처리했는가?
	- submitForm에서 GET방식으로 successUrl을 보냄.
	- 처음에 팝업으로 띄우는가?
		- openWindow로 /auth/agreements로 이동 
			- auth/agreements는 웹만 이용하는가?
				- 
		- submitForm으로 드림시큐리티 이동()
	- 기존 auth/result (웹용) 동작 방식
		- 성공 처리
		- callbackUrl이 있으면 location.replace(callbackUrl)
		- callbackUrl이 없고 window.opener 있으면 
				- 리로드하고 닫음.
				- 얼리리턴
		- callback URL이 있으면
- 기존에 앱(웹뷰)은 어떻게 처리했는가?



- 앱쪽에서 본인인증 들어올 때 여러군데에서 들어올 수 있음
	- 마지막에 set해서 정보주잖아. 
	- 그냥 닫기..

- 빌링
	- 윈도우로 떴을떄는 고렇게 해도됨.
	- 현재 빌링은 리다이렉트 시켜서 우리한테 옴.
	- 빌링은 리다이렉트로 온다. 
	- 빌링은 

웹,앱 모두 다 새창 띄우는 상황
리다이렉트없는상황이면 걍 닫으면 됨.
앱도 리다이렉트 받아야할 수 있음.
빌링쪽에서 들어오듯이, 쿠폰히스토리나 리다이렉트나 바로 들어올 수 있음.
쿠폰등록 웹뷰니까. 이동할 떄 




- 