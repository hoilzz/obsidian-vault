JS의 [import](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/import) 컨셉을 사용해서  JS 파일에서 CSS를 임포트 할 수 있다.
next 빌드 시 프로덕션 모드에서 모든 CSS 파일은 미니파이드된 .css 파일로 연결된다.

---
- `<link>` 태그와 다른점
- link 와 비교하여 누가 먼저 불리는가
- 렌더링 블락은 없는가?


---
현재 700, 400 만 사용중이라 pretendard-bold, pretendard-regular 만 가져옴