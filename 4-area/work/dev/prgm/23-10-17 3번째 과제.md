--- 

creation date: 23-10-17 
modification date: 23-10-17 
tag: 

---
- 주제 거리 
	- 리뷰 형태 질문형태로 바꿈
	- 컨벤션(파일구조, 변수명, 대문자 등)
		- 파일구조
			- 재훈님: 디렉토리 신경 안씀. 
	- 상태를 어디서 관리하고 처리하냐
		- 의존성 역전, 의존성 주입
	- validation은 타입검사.. 해도 될거 같다. 
	- throw new Error 좀 과한거 같다. 이건 서비스 코드임.
	- shallow copy, deep copy -> immer


- immutable
- 여러 객체가 뎁스있게 있으면 깊은복사 힘듬. 구현하거나 immer써
```
|   |
|---|
|const newList = [|
|||...todolist.state,|
|||{ text, isCompleted: false, id:++this.state.id }|
|||]|
```
- 에러처리
	- error 객체 그 자체를 console.error로 표현
		- 에러 객체에 정보가 많다. 에러 스택에 몇번째 라인 에러인지 쉽게 파악 가능
		- 현업에서는 sentry 라는 걸 사용하는데 이곳에서 error를 수집
- 추상화
	- localStorage와 같은 웹 API는 스펙이 정해져있음
```
받은 값을 stringify하는 동작은 Web Storage의 스펙에서 기인한 사실이라 왠만하면 바뀌지도 않고 빼먹어서도 안되는 사실인 것 같습니다.

이런 경우는 stringify 동작을 `setItem` 내부에 추상화해서 해당 메소드를 사용하는 개발자가 stringify 동작을 아예 신경쓰지 않고 "로컬스토리지에 값을 입력한다"라는 동작에만 집중할 수 있도록 만들어줘도 좋을 것 같아요!

이러면 개발자가 실수로 stringify를 안 해버리는 실수를 방지할 수도 있을 것 같구요 ㅎㅎ
```
- 리액트의 좋은점 vs 리액트 없이 개발자가 직접 다룬다
	- 최적화 하기 쉬움
	- 단점: 신경쓸게 많고 휴먼에러 발생 쉬움. 
- style태그
	- 점수가 높음. 오버라이딩 불가.
	- 그래서 classname으로 스타일 적절하게 추가하자.
- 클래스네임으로 뭔가 구분
	- 클래스 명이 변경되면 이벤트 핸들러 스트링도 변경되어야함.
	- 이거는 놓치기 쉬움. 즉 변경에 용이하지 않고 사이드이펙트 발생하기 쉬움.
	- 클래스 명이 변경되어도 핸들러 동작이 바뀌지 않게 할 방법이 있지 않을까?
- 스트링 자체로 관리하면 단순 문자열이라 오타, 태그가 잘못 닫히는 문제 등 오타나기 쉬움.
	- createElement, appendChild를 사용하여 DOM객체를 생성하면 이런 실수 줄일 수 있음.
- id가 아닌 span과 같은 dom으로 뭔가 하는거
	- 영향범위가 크고 어디서 어떤 사이드이펙트가 나올지 예상하기 힘듬.
	- 최대한 좁고 명확하게 해야함
- react의 state co-location
	- 상태 쓰는 쪽과 선언하는쪽 가까이 두어 응집도 높게 설계
	- 근데 상태 한곳에 너무 몰아둬도 문제
		- 상태가 비대해져서 이해하기 어려워짐 
		- 부모에게 상태 올릴 때는 이 상태가 다른 컴포넌트에서 공유 되어야 하는가? 위로 올림



