---
created: 24-01-23
modified: 24-01-23
tags:
---
- 동작 옥의티
    - 로딩바 구석
    - CORS이슈 서버쪽에서 해주지 못할 것 같으면 어떻게 하는게 가장 빠른 해결책일까?
    - submit 중에는 중복 제출 막기 -> debounce, 로딩 중 disabled
    - sign-up API 이후로 create API만 호출함
        - 이런건 사실 개발자 레벨에서 테스트가 잘 되어야함
    - 지금 과제까지 happy path만 테스트 해봤을 거다. 유저는 정말 다양한 방식으로 사용한다. happy path 외에 시나리오에 대해서도 테스트를 직접해봐야한다. 이제는 구현뿐만 아니라 테스트 코드 혹은 테스트 시나리오에 대해서도 고려해볼 때. + 이러한 것을 고려해놓으면 유지보수 운영 할 때 해당 코드를 수정하게 될 경우 이전 동작이 그대로 동작해야하는데 테스트 코드가 작성되어있다면 이전 동작에 대한 유지를 보장할 수 있음. (https://fe-developers.kakaoent.com/2022/220825-msw-integration-testing/)
    - 다른 유저 만든 디데이가 로그인시 디데이가 남아있음;
    - 로그인 상태에서 게시판 리스트에서 하트 누르면 로그인이 풀림



- 코드를 볼께요
    - dotenv..!
    - api 호출 함수와 응답 타입이 분리되어있는데 이렇게 한 이유가 있을까요? 어떤 이점이 있었는지 혹은 이렇게 결정하게 된 이유 개인적으로는 api와 타입이 응집도 높게 한파일에 있는 것을 선호합니다. 
    - react-query v5부터(왜 v3을 쓰셨는지 궁금하네요) callback은 전부 제거될 예정입니다. 메인테이너가 이렇게 결정하게된 의도를 한번씩 살펴보고 이 의견에 어떻게 생각하는지 자신의 생각도 정리해보세요. react-query 뿐만 아니라 메이저 업데이트의 릴리즈 노트는 슥 한번 봐주는게 좋아요. 특히 breaking change에는 항상 이렇게 변경하게된 이유가 적혀있는데 한번씩 보는 것을 추천드려요. 다른 개발자의 생각을 염탐하면서 자신의 코드에도 적용할 수 있습니다. https://github.com/TanStack/query/discussions/5279
    - UserContentBlock, fantasize number|string 이렇게까지 고려할 필요 없다. 팀내에서 사용하는거니 우리 fontsize는 string으로만 합시다! 땅땅 이렇게 하는게 강제적이고 오히려 편하다. 사용하는 사람 입장에서 고민없이 정해진 컨벤션으로 사용하는 것도 DX관점에서 중요.
    - common의 공통컴포넌트는 재사용성을 높이는 코드인데 API 호출 로직 책임도 가지게 되면 재사용성이 낮아지게 되지 않을까? PostList.tsx + 이걸 가지고 스토리북을 만든다면 APi에 대한 대응코드를 추가해줘야해서 유지보수 하기 귀찮은게 많아지지 않을까?
    - 전체적으로 느껴진건데 코드가 너~무 길다(스크롤을 너무 많이 한다) 싶으면 한번쯤은 이 컴포넌트 내부에서 무슨무슨 일이 벌어지고 있지? 하면서 리스트화 후에 적절하게 추출하는 것도 좋을 것 같습니다.

- 좋았어요
    - api, hook, type 네이밍과 책임 분리가 적절하게 잘된 것같다
    - 배포시에 릴리즈브랜치와 버저닝까지 했으면 더 완벽한 배포 플로우였을 것 같다
    - 매직넘버를 적절하게 상수화 잘하신 것 같다.
    - 업무 분리가 잘 되어 프로젝트 진행에는 무리가 제가보기에는 없어보였어요(?). 팀원분들 의견도 궁금하네요!
    - 에러 처리하기 어려운 상황에서 (API에서 에러 발생시 어떤 에러인지 알기 어려움) 최대한 처리해보려고 노력하신점. 그리고 에러에 많은 고민을 하신점 좋았습니다. 
    - PR에서 서로서로 코드에 대해 논의하고 맞춰나가는 모습도 실제 팀 협업이 잘 워킹하는 걸로 보여서 좋았습니다.