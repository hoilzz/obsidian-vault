### 원인

1. 리렌더링
- 상위 루트로 SubTab을 올렸을 때는 스크롤이 0으로 이동하지 않음.
	- 리렌더링은 props가 바뀌면 발생하는건데 
	- ~~리렌더링과 언마운트는 무관하다~~

2. getServerSideProps인 페이지에서 router.push 동일페이지 with 다른 쿼리스트링일 때 section이 unmount 되는 현상
	- 해당 페이지 요청시 props와 함께 페이지가 pre-rendered 
	- next/link 나 next/router로 클라이언트 사이드 요청시, next는 getserversideprops를 실행하는 서버에서 API 요청을 보냄.
	- __페이지를 렌더하는데 사용될 JSON 을 리턴한다.__ 이 모든 작업은 Next에서 자동으로 처리한다. 그래서 뭐 할게 없음.
	- 