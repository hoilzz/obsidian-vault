---
created: 25-01-19
modified: 25-01-19
tags:
---
이 문서의 목표는 이력서 리뷰, 과제 코드 리뷰, 모의 면접 등에 대해 프로세스를 만드는 것이다. 이 문서만 읽고도 멘토링을 순차적으로 진행할 수 있어야 한다.
## 1. 자기소개

안녕하세요. 카카오 웹툰 서비스 개발하고 있는 7년차 FE개발자 입니다. 그 전에는 커머스 쪽에서 일해본 경험이 있습니다.

## 2. 지원자의 현재 상황과 목표 체크
-   현재 많은 면접을 진행 하였는데 기술, 인성 면접에서 10차례 정도 떨어졌습니다. (네이버, 카카오, 두나무, 당근, 토스, 트래블월렛, 타다 등등)  
- 이 부분에서 제가 기술적이나 연차에 대한 부족함이 있어 떨어지는 부분도 물론 있지만 핏에 대한 부분(말투나 다른 부분 포함) 모의 면접을 신청하게 되었습니다.  
- 회사 내에선 제 권한 내에서 도입할 수 있는 최대치를 했다고 생각합니다. 당분간은 그 부분은 힘들 거 같아 추가 학습을 하는데 있어서 회사에 도입을 하는 것은 애로사항이 있는데요.
- 추가적으로 어떤 부분에서 학습을 하고 이력서나 면접에서 어필을 하면 좋을지 궁금하기도 합니다.  
	- gitops? -> k8s + argocd -> 부하테스트
- 또한 뽑히는 사람의 말투나 행동거지는 어떤지에 대한 인사이트도 좀 얻고 싶습니다.
## 3. 이력서 리뷰

### 3-1. 실무에서 이력서를 보는 프로세스
- [[이력서 검토 프로세스]]

### 3-2. 이력서 리뷰
- 전달 받은 이력서를 pdf to markdown 한다
- 지원자가 작성한건 italic, 그 아래 들여쓰기를 통해 수정사항, 질문사항을 작성한다.
### 3-3. 이력서 총평
- 전체적인 리뷰
### 4-1 포트폴리오 / 깃헙 리뷰 포인트
[[멘토링 for#이력서 리뷰에서 내가 해줘야할 일#포트폴리오, 깃헙 코드 리뷰]]

### 4-2 질의응답
- _미디어 리소스를 동적으로 관리하여 메모리 사용량 최적화_
	- how?
- _기존 컨텍스트 기반 로직을 -> 동영상 재생이 라우팅에 영향을 받지 않도록 리팩토링_
	- 기존에는 컨텍스트 기반이라 라우팅에 영향? 이해가 잘 안됨
	- _병렬 라우팅 구조란? 패러럴 라우트?_
- _SSG, ISR 적극 활용_
	- 어떤 페이지에 왜 적용했는지
	- 이거 혹시 revalidateTime은 어떻게 되는지?
		- Time-based ISR 이면 
			- 어떤 단점이 있을까?
				- 주기가 짧을수록 비효율적인 API 요청 많아짐
				- 만약 5분이라면 변경사항 관리자가 변경사항 확인하려고 5분 기다려야함. 
			- 만약에 실시간으로 캐싱 초기화 해야되는 요구사항은 없었을까?
				- 있으면 어떻게 해결하면 좋을까? __On-Demand ISR__
				- 뭔가 트리거하는게 있으면 좋지 않을까?
					- 근데 이방식도 단점이 있지 않을까? 멀티인스턴스 상황에서.. 인스턴스가 1대만 떠있지 않을꺼니까..
					- 일일이 revalidate -> scale out시 그만큼 회수도 늘어나고, 로드밸런서때문에 어느 인스턴스에 진입할지 모르는 상태에서 데이터를 비교해 stale 인스턴스를 찾는 등... (즉, 멀티인스턴스 내에서 하나의 인스턴스 캐시만 퍼지 가능하기 떄문에 다른 인스턴스 데이터 동기화가 되지 않는 문제가 있음.)
						- 파일시스템 기반 캐시(인스턴스 로컬 디스크나 메모리)
						- 그리고 revalidatePath 같은 On-demand API 호출시
							- 현재 요청을 받은 인스턴스의 캐시만 무효화됨
							- 즉, 로드밸런서가 라운드로빈/랜덤 방식으로 B 인스턴스 보내면 A,C,D,E 인스턴스 캐시 남아있음.
							- 기본적으로 머맅 인스턴스 구조는 각 인스턴스가 격리된 환경에서 실행.
							- 디스크도, 메모리도 .next/cache도 서로 공유안함.
							- 따라서 한 인스턴스 캐시 무효화해도 다른 인스턴스 그 사실 알 수 없음.
							- for문 돌리면 안됨?
								- 인스턴스 주소(IP/도메인) 알 수 없음. 로드 밸런서 뒤에 숨어서..
								- 일반적으로 퍼블릭 IP가 없음. 하나의 공용URL로만 접근 가능.
								- 인스턴스 HTTP호출 하려면 내부 API 열어야하는데 보안 이슈
								- 인프라 복잡도 증가: 인스턴스 수가 가변, 새로운 인스턴스 붙으면 또 업데이트..
				- 인스턴스 마다 프레시 상태를 만들지 말고 캐시상태를 공유하면 어떨까?
					- 레디스를 이용하여 공통 캐시 저장소. (레디스는 공유 캐시 스토리지. 멤캐시드)
					- 특정 인스턴스에서 revalidate실행시 레디스에 있는 캐시가 퍼지, 새롭게 셋
			- revalidation 
				- 
					- 
					- 
- 부하테스트 메트릭 관련..
	- TPS나 CPU 사용률 개선혹시 봤었나 (우리는 5~10배까지 캐싱으로 개선됨. TPS 45 -> TPS 220)
	- CPU 튈 떄 어디를 가장 먼저 의심할건지
		- SSR, ServerCOmponent 무거운 계산 의심 
			- 이쪽 내부 코드에 파일 IO나 뭔가 무거운 작업 찾을 것 같다.
			- HTTP쪽 클라이언트 요청마다 새로운 TCP 연결 
	- 메모리 사용량 급증시
		- 정적 캐시 급증 했거나.. 
		- GC가 돌지 못하는 상황
			- 참조 객체가 해제되지 않는다.
			- 

- "HTTP란 무엇인가?"
	- "비동기 제어는 왜 필요한가?"
		- > **"어떤 작업을 요청하고, 그 결과가 오기 전에 다음 코드를 계속 실행할 수 있는 방식"**
		- ### 1. **사용자 경험(UX) 개선**
- 서버에서 데이터를 받아올 때 기다리면 화면이 멈춤
- 비동기로 처리하면 **로딩 상태를 표시하면서 사용자에게 반응 가능**
### 2. **브라우저는 단일 스레드 (Single Thread)**
- 자바스크립트는 한 번에 한 줄씩 실행됨
- **비동기 처리 없으면 대기 중인 작업이 다른 작업을 막음 → UI 멈춤**
	- "비동기 처리는 네트워크 요청, 타이머, 사용자 입력 같은 이벤트를 처리할 때 쓰입니다. 자바스크립트는 싱글 스레드 기반이라 비동기가 없으면 UI가 멈추거나 응답이 느려집니다. 저는 대부분 `async/await` 기반으로 처리하고, 병렬 처리나 에러 핸들링이 필요한 경우엔 `Promise.all`, `try/catch` 패턴을 같이 씁니다."


-  immutable이 mutable보다 나은 이유
	- mutable은 상태 추적 어려움 
		- ### **예측 가능성과 디버깅 용이성**

> Immutable한 데이터는 **값이 바뀌지 않기 때문에**, 원인을 추적하거나 상태 변경을 디버깅할 때 매우 유리함.

#### 🧠 예시:

> "불변 데이터는 이전 상태를 그대로 유지하므로, 상태 변경이 언제 어디서 발생했는지 추적이 쉬워집니다. 특히 시간여행 디버깅이나 undo/redo 같은 기능도 구현하기 좋아요."
- 선언형에 대해 어떻게 생각하시나요?
	- "선언형 프로그래밍은 **어떻게(How)**가 아니라 **무엇을(What)** 할지를 기술하는 방식입니다. 예를 들어 React의 JSX는 DOM 조작을 직접 하지 않고 '이렇게 생긴 UI를 원한다'고 선언하죠. 반대로 명령형은 `document.createElement`나 `appendChild`처럼 직접 DOM을 조작합니다."
	- ### 2. **"React는 왜 선언형이라고 하나요?"**
#### 🌟 답변 예시:

> "React는 상태가 바뀌었을 때 UI가 어떻게 바뀌어야 하는지를 선언적으로 표현할 수 있게 해줍니다. 실제 DOM을 어떻게 갱신할지는 React 내부의 Virtual DOM과 reconciliation 알고리즘이 처리하죠. 저는 이런 선언형 방식 덕분에 UI 상태를 예측 가능하게 만들 수 있다고 생각합니다."

### 3. **"선언형이 항상 좋은가요?"**

#### 🌟 답변 예시:

> "항상 좋은 건 아니라고 생각합니다. 복잡한 애니메이션, 렌더 타이밍 제어가 중요한 작업, 로우레벨 최적화가 필요한 경우엔 오히려 명령형이 더 명확하고 효율적일 수 있어요. 실제로 저는 IntersectionObserver나 canvas 작업에서는 명령형을 선택하는 편입니다."


### 4. **"최근에 선언형으로 리팩터링해서 얻은 장점이 있었나요?"**

#### 🌟 답변 예시:

> "예전에 `setTimeout`, `addEventListener`를 직접 다루는 컴포넌트를 `useEffect + 상태 기반 렌더링`으로 바꾼 적 있습니다. 선언형으로 바꾸니까 **사이드 이펙트 관리가 쉬워졌고**, 버그가 줄었어요. 특히 클린업이 자동화돼서 메모리 누수도 방지됐습니다."

- 데이터 1000개 페이지 10개.. 매번 천개를 

- 공통 로직 추상화
	- 추상화에 대해 설명해주세요
		- "추상화는 **복잡한 시스템을 단순화하여 핵심만 드러내는 과정**입니다. 프론트엔드 개발에서는 반복되는 UI나 비즈니스 로직을 공통화해서 재사용 가능하게 만드는 것이 대표적 예시입니다. 이렇게 하면 유지보수가 쉬워지고, 팀원 간 커뮤니케이션도 명확해집니다."
		- > 단순한 기능ㅈ정도 뺴는거
		- fetch API
			- axios 인스턴스를 만드는 부분 분리하고
				- API따로 호출하고
				- service API
				- type도 분리
				- 각각의 의존성을 분리.
				- 비대해졌다 -> 유지보수나 생산성이 떨어지는거 같다. OCP
	- 실제 업무에서 추상화를 어떻게 사용하시나요?
		- 디자인 시스템 구축 시 Button, Input 같은 **Atomic 컴포넌트를 추상화**하고, 공통 인터랙션과 스타일을 분리해 재사용 가능한 컴포넌트를 만듬. 또한 React Query와 Zustand를 사용할 때도 **비즈니스 도메인 단위로 훅을 추상화**해, API 호출과 상태 관리가 분산되지 않도록 함
			- _"추상화된 컴포넌트에서 하위 컴포넌트와의 결합도를 낮추기 위해 어떤 전략을 사용하나요?"_
	- 추상화가 오히려 복잡해진 경험?
		- 과도하게 추상화하면 오히려 컴포넌트 구조가 너무 깊어지고, 디버깅이 어려워지는 문제가 있었습니다. 그래서 팀에서는 '3번 이상 반복되는 로직만 추상화하자'는 기준을 세우고, **과한 일반화보다는 실용적인 추상화**를 지향했습니다.
	- "추상화의 단점은 무엇이며, 언제 피해야 한다고 생각하나요?"
		- 추상화의 가장 큰 단점은 **의도 파악이 어려운 코드가 될 수 있다는 점**입니다. 실제 구현이 추상화 뒤에 숨어버리기 때문에, 신규 팀원이 진입장벽을 느낄 수 있습니다. 특히 **단 한 번밖에 사용되지 않는 컴포넌트를 추상화하거나, 도메인이 명확하지 않은 상태에서 추상화**하면 오히려 리팩터링 비용이 커질 수 있습니다.
	- _"추상화와 OCP(개방-폐쇄 원칙)의 관계에 대해 설명해보세요."_
- 다국어지원
	- 업무 설명해주세요.
		- 사용자경험측..
- 모노레포 구축이유?
	- 모노레포의 장점
		- 이걸 안썼을 때 역체감.
			- 일관성있는 
		- 기술
		- 협업
			- 소프트웨어 구조는 해당 소프트웨어를 개발한 조직의 커뮤니케이션 구조를 닮게 된다 콘웨이 법칙
				- 소프트웨어 구조가 조직의 커뮤니케이션 구조와 다르다면 어딘가 잘못된 구조다
			- 목적 조직에 맞게.. 저희 팀의 지향점중 하나는 누가 퇴사/휴가 가더라도 모든 서비스에 대응 가능.
			- - 유사한 기능을 추상화하고 공유 패키지화하여 중복 작업을 줄일 수 있어요.
				- 모든 사람이 모든 것을 보고 더 많이 저장소에 참여하면서 함께 한다는 “느낌”, 제품에 대한 “소유권”이 발생됩니다. 이는 자발적으로 기술에 대한 집착과 지식을 공유하게 돼요.
				- 그리고 회사에서 표준화하려는 환경과 배포 관리가 쉬워요.
				    - 예를 들어 package.json 과 같은 종속성 관리나
				    - 공유된 패키지의 재사용(예를들어 /design-system 또는 /common-util, /schema)을 쉽게 할 수 있고
				    - 환경설정(ESlint, TSConfig 등)을 구성하거나
				    - 테스트(unit, e2e)환경을 공통으로 구성하기 좋아요.
				    - https://green-labs.github.io/monorepo-microfrontend
- __인성__
	- 기술적인 의사결정
		- 나와 동료의 역량, 비즈니스 임팩트 등 다양한 상황 고려하여 결정. 가끔 잘만들고 싶어도 포기해야하는 경우도 있다
		- 아름다운 코드 작성이 1순위가 아님. 코드로 사람들에게 가치를 전달하고 여기서 비즈니스 임팩트를 내야함. 회사에서의 1순위는 무조건 비즈니스
	- 나와 같이 일하는 동료가 조금 업무 속도가 느리거나 코드퀄리티가 낮다 어떻게 할것인가?
		- __보인다__ 
			- 일정이 가장 중요. 
	- 코드 스타일/ 방향성이 다른 경우
	- 이전회사에서 가장 아쉬웠던 점
	- 최근에 가장 크게 성장했다고 느낀점
	- 좋은 개발팀의 모습?
		- __컨벤션이 이미 롤로써 자리가 다 잡혀있어서__ 코드 품질에 대해 보장이 되어있으면 좋겠다. 서로가 팀원을 다 믿는 형식. 문제가 생기더라도 (문제를 )
	- 이직 결심한 이유?
		- __
	- 내가 해결할 수 있는거
		- __반복적이넉..__ 
	- 가장 중요하게 생각하는 회사문화
	- 회사 결정이 본인과 맞지 않는다면?
### 5 앞으로 업무 혹은 학습 방향
[[멘토링 for#이력서 리뷰 후에 앞으로의 방향성]]

### 6 질의응답
- 내가 작성

### 7 추가로 해줄 말들
- [[개발자에 필요한 역량]]
	- [[테스트 코드]]
	- 컴포넌트의 추상화를 통한 공통 컴포넌트 제작
		- 헤드리스 UI부터 천천히..
	- CI, CD 구축 (gitops)
	- 협업간 발생하는 문제를 해결
		- msw
		- DTO 작성 효율화 (open api generator)